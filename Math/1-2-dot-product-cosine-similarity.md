# 내적과 코사인 유사도

> 학습일: 2026-08-05

## 1. 오늘 학습 키워드

- 내적(Dot Product)
- 코사인 유사도(Cosine Similarity)
- 벡터의 방향
- 벡터의 크기와 노름
- 임베딩(Embedding)
- 유사도 검색
- NumPy의 `np.dot()`과 `@`
- `cosine_similarity()`
- Pandas의 `sort_values()`

---

## 2. 오늘 학습한 내용

### 내적의 정의

내적은 차원이 같은 두 벡터의 같은 위치에 있는 성분끼리 곱한 뒤, 그 결과를 모두 더하는 연산이다.

두 벡터가 다음과 같을 때,

$$
\mathbf{a} =
\begin{bmatrix}
a_1 \\
a_2 \\
\vdots \\
a_n
\end{bmatrix},
\qquad
\mathbf{b} =
\begin{bmatrix}
b_1 \\
b_2 \\
\vdots \\
b_n
\end{bmatrix}
$$

내적은 다음과 같이 계산한다.

$$
\mathbf{a}\cdot\mathbf{b} =
a_1b_1+a_2b_2+\cdots+a_nb_n
$$

내적의 결과는 벡터가 아니라 하나의 스칼라 값이다.

```python
import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

dot1 = np.dot(a, b)
dot2 = a @ b

print(dot1)
print(dot2)
```

출력:

```text
32
32
```

`np.dot(a, b)`와 `a @ b`는 이 예제처럼 두 입력이 1차원 벡터일 때 같은 내적 결과를 반환한다. 고차원 배열에서는 두 연산의 동작 방식이 달라질 수 있다.

---

### 내적의 기하학적 의미

두 벡터 $\mathbf{a}$와 $\mathbf{b}$ 사이의 각도를 $\theta$라고 하면 다음 관계가 성립한다.

$$
\mathbf{a}\cdot\mathbf{b} =
\|\mathbf{a}\|\|\mathbf{b}\|\cos\theta
$$

이 식은 내적이 두 벡터의 크기뿐만 아니라 두 벡터가 이루는 각도와도 관계가 있다는 것을 보여준다.

0이 아닌 두 벡터의 내적 부호는 두 벡터가 이루는 각도를 해석하는 데 사용할 수 있다.

- 내적이 양수: 두 벡터 사이의 각도가 90도보다 작다.
- 내적이 0: 두 벡터가 서로 수직이다.
- 내적이 음수: 두 벡터 사이의 각도가 90도보다 크다.

다만 내적 값은 벡터의 방향뿐 아니라 벡터의 크기에도 영향을 받는다.

---

### 코사인 유사도

내적을 두 벡터 크기의 곱으로 나누면 두 벡터가 이루는 각도의 코사인 값을 얻을 수 있다.

$$
\cos\theta =
\frac{
\mathbf{a}\cdot\mathbf{b}
}{
\|\mathbf{a}\|
\|\mathbf{b}\|
}
$$

이 값을 코사인 유사도라고 한다. 코사인 유사도는 두 벡터가 모두 영벡터가 아닐 때 정의된다.

```python
def cosine_similarity(a, b):
    return np.dot(a, b) / (
        np.linalg.norm(a)
        * np.linalg.norm(b)
    )
```

코사인 유사도는 벡터의 크기 차이를 제거하고 방향이 얼마나 비슷한지를 비교하는 데 사용한다.

0이 아닌 실수 벡터의 코사인 유사도는 -1 이상 1 이하이며, 다음처럼 해석한다.

- `1`: 두 벡터가 같은 방향
- `0`: 두 벡터가 서로 수직
- `-1`: 두 벡터가 완전히 반대 방향

코사인 유사도 값이 클수록 두 벡터의 방향이 비슷하다.

두 벡터를 각각 정규화한 뒤 내적해도 같은 값을 얻는다.

$$
\cos\theta =
\left(\frac{\mathbf{a}}{\|\mathbf{a}\|}\right)
\cdot
\left(\frac{\mathbf{b}}{\|\mathbf{b}\|}\right)
$$

---

### 임베딩과 유사도 검색

임베딩은 텍스트나 이미지를 숫자 벡터로 변환한 것이다.

임베딩 공간에서는 의미가 비슷한 대상일수록 벡터의 방향이 비슷하도록 학습될 수 있다.

따라서 질문 벡터와 여러 문서 벡터의 코사인 유사도를 계산하고, 유사도가 높은 문서를 찾으면 질문과 관련 있는 문서를 검색할 수 있다.

같은 원리를 이용해 사용자가 선택한 상품과 유사한 상품을 추천할 수도 있다.

코사인 유사도는 다음 분야의 기초가 된다.

- 임베딩 검색
- 추천 시스템
- 검색 엔진
- 어텐션 메커니즘

---

### 내적과 코사인 유사도의 차이

내적은 벡터의 크기와 방향에 모두 영향을 받는다.

따라서 내적 값이 크다고 해서 반드시 두 벡터의 성향이나 방향이 더 비슷하다고 단정할 수는 없다.

예를 들어 고객별 구매 수량을 벡터로 표현했을 때 구매량이 많은 고객은 전체 성분의 크기가 크기 때문에 다른 고객과의 내적도 크게 나올 수 있다.

반면 코사인 유사도는 두 벡터를 각각의 크기로 나누어 방향을 중심으로 비교한다.

구매량의 절대적인 크기보다 구매 품목의 비율이나 성향을 비교하려면 코사인 유사도가 더 적합할 수 있다.

---

## 3. 나만의 정리

내적은 두 벡터에서 같은 위치의 성분끼리 곱한 뒤 모두 더한 값이다.

내적의 결과는 스칼라로 나오며, 이 하나의 숫자를 이용해 벡터 사이의 관계를 비교할 수 있다.

다만 내적은 벡터의 크기에도 영향을 받기 때문에, 값이 크다고 해서 두 벡터의 성향이나 의미가 반드시 비슷하다고 보기는 어렵다.

그래서 방향의 유사도를 비교할 때는 코사인 유사도를 사용한다.

코사인 유사도는 두 벡터가 같은 방향인지, 수직인지, 반대 방향인지를 확인할 수 있다.

코사인 유사도를 구하려면 두 벡터의 내적을 각 벡터 크기의 곱으로 나눈다.

$$
\text{cosine similarity} =
\frac{
\mathbf{a}\cdot\mathbf{b}
}{
\|\mathbf{a}\|
\|\mathbf{b}\|
}
$$

아직 이 공식이 왜 이런 형태가 되는지는 완전히 이해하지 못했지만, 내적의 기하학적 공식에서 $\cos\theta$를 구하도록 식을 정리한 결과라고 이해했다.

임베딩은 텍스트나 이미지를 숫자 벡터로 변환하는 과정이다.

텍스트나 이미지의 유사도를 계산하려면 먼저 비교할 대상을 벡터로 표현해야 하므로 임베딩이 먼저 필요하다.

코사인 유사도가 0이면 두 벡터가 수직이라는 뜻이다. 임베딩에서는 의미적으로 관련이 적다고 해석할 수 있지만 항상 절대적으로 무관하다는 뜻은 아니다.

`a @ b`는 내적을 수학적으로 표현하는 기호처럼 보였지만, Python과 NumPy에서는 실제 연산자로 동작한다는 점이 인상적이었다.

내적이 클수록 전체 구매량이나 구매력 같은 크기는 크게 반영될 수 있지만, 상품별 단위 차이나 전체 구매량의 영향을 받기 때문에 구매 성향이나 선호도를 비교하기는 어려울 수 있다.

자기 자신과의 코사인 유사도는 1이므로 가장 높은 값이 나온다. 반면 자기 자신과의 내적은 자신의 노름을 제곱한 값이지만, 크기가 더 큰 다른 벡터와의 내적보다 항상 크다고 할 수는 없다.

코사인 유사도로 비슷한 다른 고객을 찾을 때는 자기 자신을 결과에서 제외해야 한다.

`np.dot()`은 직접 성분별 곱셈과 합을 작성하는 것보다 내적을 간단하게 표현할 수 있는 기능이다.

```python
def cosine(a, b):
    return np.dot(a, b) / (
        np.linalg.norm(a)
        * np.linalg.norm(b)
    )
```

코사인 유사도는 위처럼 직접 구현할 수도 있지만, 라이브러리의 `cosine_similarity()` 기능을 사용할 수도 있다.

---

## 4. 실습

고객별 상품 구매 데이터를 벡터로 표현하고, 기준 고객과 다른 고객 사이의 내적과 코사인 유사도를 계산했다.

내적 값만 사용할 때는 구매량이 많은 고객의 값이 크게 나올 수 있다는 점을 확인하고, 코사인 유사도를 이용해 벡터의 방향을 중심으로 구매 성향을 비교했다.

또한 기준 고객 자신과의 코사인 유사도가 1로 나오기 때문에, 가장 유사한 다른 고객을 찾을 때는 자기 자신을 제외해야 한다는 점을 확인했다.

- [내적과 코사인 유사도 실습 코드](./1-2-dot-product-cosine-similarity-Practice.py)

---

## 5. 학습하며 겪었던 문제점과 해결 과정

### 코사인 유사도 공식을 이해하기 어려웠다

코사인 유사도를 구할 때 내적을 두 벡터 노름의 곱으로 나누는 이유가 바로 이해되지 않았다.

내적의 기하학적 정의는 다음과 같다.

$$
\mathbf{a}\cdot\mathbf{b} =
\|\mathbf{a}\|\|\mathbf{b}\|\cos\theta
$$

이 식의 양변을 $\|\mathbf{a}\|\|\mathbf{b}\|$로 나누면 다음 식이 된다.

$$
\cos\theta =
\frac{
\mathbf{a}\cdot\mathbf{b}
}{
\|\mathbf{a}\|
\|\mathbf{b}\|
}
$$

따라서 코사인 유사도 공식은 별도의 새로운 공식을 외우는 것이라기보다, 내적의 기하학적 식에서 $\cos\theta$를 구하도록 정리한 결과이다.

---

### 내적이 크면 유사도가 높은 것인지 헷갈렸다

내적은 벡터의 방향뿐 아니라 크기에도 영향을 받는다.

구매량이 많은 고객은 여러 성분의 값이 크기 때문에 다른 고객과의 내적도 크게 나올 수 있다.

따라서 절대적인 구매 규모를 비교할 때는 내적이 의미가 있을 수 있지만, 구매 성향이나 방향을 비교할 때는 코사인 유사도가 더 적절할 수 있다.

---

### 자기 자신과의 코사인 유사도가 가장 높게 나왔다

0이 아닌 벡터와 자기 자신의 코사인 유사도는 1이다. 이는 코사인 유사도의 최댓값이므로 자기 자신이 가장 높은 값으로 나온다.

다른 고객 중 가장 비슷한 고객을 찾으려면 자기 자신의 행을 제거하거나 제외한 뒤 정렬해야 한다. 내적은 벡터 크기의 영향을 받기 때문에 자기 자신과의 값이 항상 가장 크다고 보장할 수 없다.

---

### `sort_values(ascending=False)`의 의미

`sort_values()`는 Pandas의 `Series` 또는 `DataFrame` 값을 기준으로 정렬하는 기능이다.

```python
similarities.sort_values(ascending=False)
```

`ascending=False`는 값을 큰 값부터 작은 값 순서로 정렬한다는 뜻이다.

- `ascending=True`: 오름차순
- `ascending=False`: 내림차순

코사인 유사도를 내림차순으로 정렬하면 가장 유사한 대상부터 확인할 수 있다.

자기 자신을 제외하고 가장 유사한 고객을 찾는 예시는 다음과 같다.

```python
sorted_similarities = similarities.drop(
    index=target_index
).sort_values(
    ascending=False
)

print(sorted_similarities)
```

---

### `cosine_similarity()` 기능

직접 만든 함수 외에도 scikit-learn에서 코사인 유사도를 계산할 수 있다.

```python
from sklearn.metrics.pairwise import (
    cosine_similarity
)

result = cosine_similarity(matrix)
```

행렬을 전달하면 각 행 벡터 사이의 코사인 유사도를 계산한 행렬을 반환한다.

---

## 정리

- 내적은 같은 위치의 성분끼리 곱한 뒤 모두 더한 스칼라 값이다.
- `np.dot(a, b)`와 `a @ b`로 벡터의 내적을 계산할 수 있다.
- 내적은 벡터의 크기와 방향에 모두 영향을 받는다.
- 코사인 유사도는 내적을 두 벡터 노름의 곱으로 나눈 값이다.
- 코사인 유사도는 벡터의 크기보다 방향의 유사성을 비교한다.
- 임베딩은 텍스트나 이미지를 숫자 벡터로 변환하는 과정이다.
- 임베딩된 벡터의 코사인 유사도를 이용해 검색과 추천을 구현할 수 있다.
- 유사도 결과를 정렬할 때 `sort_values(ascending=False)`를 사용하면 큰 값부터 확인할 수 있다.
- 유사한 다른 대상을 찾을 때는 자기 자신을 비교 대상에서 제외해야 한다.

